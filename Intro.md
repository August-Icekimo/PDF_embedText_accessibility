【快速操作指引】
1. 指定任務：將需要處理的背景檔案 (就是你掃描器產生的PDF / JPG 等純圖檔內容，過不了語音報讀的PDF) 直接拖曳到此視窗，或是使用上方按鈕瀏覽。
2. 自動配對：若同一資料夾下有名稱相同的 JSON 結構檔，系統會自動載入。
3. 手動配對：若狀態顯示「⚠️ 缺 JSON」，請在列表中雙擊該筆「JSON 檔案」欄位進行手動指定。
4. 設定輸出：在下方「輸出目錄」填入您想存檔的位置。
5. 產出 PDF：所有任務皆備妥 JSON 後，點選右下角的「▶ 全部執行」即可開始輸出無障礙 PDF。

【提示】
想要讓 AI 幫忙辨識圖片生成 JSON 結構檔，點選上方「📋 複製」我們的「AI 輔助 Prompt」，貼給具有多模態視覺能力的 AI 模型使用。推薦使用 Gemini 3 Pro 或更新版本。本工具產生的 JSON 結構檔僅供參考，實際輸出 PDF 的無障礙程度仍需人工檢查(例如臺灣推薦的NVDA報讀工具免安裝版)。
